import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	// root: '.',
	// build: {
	// 	outDir: '../docs2',
	// },
	plugins: [sveltekit()]
});
